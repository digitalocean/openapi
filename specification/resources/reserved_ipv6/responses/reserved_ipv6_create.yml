description: The response will be a JSON object with a key called `ip` and the `region_slug`.


headers:
  ratelimit-limit:
    $ref: '../../../shared/headers.yml#/ratelimit-limit'
  ratelimit-remaining:
    $ref: '../../../shared/headers.yml#/ratelimit-remaining'
  ratelimit-reset:
    $ref: '../../../shared/headers.yml#/ratelimit-reset'

content:
  application/json:
    schema:
      type: object
      properties:
        reserved_ip:
          $ref: '../models/reserved_ipv6.yml'
        links:
          type: object
          properties:
            droplets:
              type: array
              items:
                $ref: '../../../shared/models/action_link.yml'
            actions:
              type: array
              items:
                $ref: '../../../shared/models/action_link.yml'

    examples:
      reserved_ipv6_reserved:
        $ref: 'examples.yml#/reserved_ipv6_reserved'