operationId: security_list_secret_versions

summary: List Secret Versions

description: To list all versions of a secret, send a GET request to `/v2/security/secrets/{secret}/versions`.

tags:
  - Security

parameters:
  - $ref: 'parameters.yml#/secret'
  - $ref: '../../shared/parameters.yml#/region'

responses:
  '200':
    $ref: 'responses/secret_versions.yml'

  '401':
    $ref: '../../shared/responses/unauthorized.yml'

  '404':
    $ref: '../../shared/responses/not_found.yml'

  '429':
    $ref: '../../shared/responses/too_many_requests.yml'

  '500':
    $ref: '../../shared/responses/server_error.yml'

  default:
    $ref: '../../shared/responses/unexpected_error.yml'

x-codeSamples:
  - $ref: 'examples/curl/security_secret_list_versions.yml'

security:
  - bearer_auth:
      - 'security:read'
