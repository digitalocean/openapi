operationId: security_list_secrets

summary: List Secrets

description: To list secrets across all configured regions, send a GET request to `/v2/security/secrets`.

tags:
  - Security

parameters:
  - $ref: '../../shared/parameters.yml#/per_page'
  - $ref: '../../shared/parameters.yml#/page'

responses:
  '200':
    $ref: 'responses/secrets.yml'

  '401':
    $ref: '../../shared/responses/unauthorized.yml'

  '429':
    $ref: '../../shared/responses/too_many_requests.yml'

  '500':
    $ref: '../../shared/responses/server_error.yml'

  default:
    $ref: '../../shared/responses/unexpected_error.yml'

x-codeSamples:
  - $ref: 'examples/curl/security_secrets_list.yml'

security:
  - bearer_auth:
      - 'security:read'
