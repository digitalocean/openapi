properties:
  cpu_usage_percent:
    format: double
    type: number
  memory_usage_percent:
    format: double
    type: number
  name:
    type: string
  replicas_desired:
    format: int64
    type: integer
  replicas_ready:
    format: int64
    type: integer
  state:
    $ref: apps_component_health_status.yml
type: object
