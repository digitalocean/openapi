properties:
  components:
    items:
      $ref: detect_response_component.yml
    title: A list of detected app components
    type: array
  template:
    $ref: apps_deploy_template.yml
  template_error:
    title: The error that is causing the template to be invalid
    type: string
    example: Error
  template_found:
    format: boolean
    title: Whether or not a deploy template was found
    type: boolean
    example: true
  template_valid:
    format: boolean
    title: Whether or not the deploy template is valid
    type: boolean
    example: true
type: object
