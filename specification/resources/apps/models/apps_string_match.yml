properties:
  exact:
    description: Exact string match. Only 1 of `exact`, `prefix`, or `regex` must
      be set.
    maxLength: 256
    minLength: 1
    type: string
    example: exact
  prefix:
    description: Prefix-based match. Only 1 of `exact`, `prefix`, or `regex` must
      be set.
    maxLength: 256
    minLength: 1
    type: string
    example: exact
  regex:
    maxLength: 256
    minLength: 1
    title: 'RE2 style regex-based match. Only 1 of `exact`, `prefix`, or `regex` must
      be set. For more information about RE2 syntax, see: https://github.com/google/re2/wiki/Syntax'
    type: string
    example: exact
type: object
