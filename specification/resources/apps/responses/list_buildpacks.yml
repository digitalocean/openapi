description: A JSON object with key `buildpacks`

content:
  application/json:
    schema:
      $ref: ../models/apps_list_buildpacks_response.yml
    examples:
      buildpacks:
        $ref: examples.yml#/buildpacks

headers:
  ratelimit-limit:
    $ref: ../../../shared/headers.yml#/ratelimit-limit
  ratelimit-remaining:
    $ref: ../../../shared/headers.yml#/ratelimit-remaining
  ratelimit-reset:
    $ref: ../../../shared/headers.yml#/ratelimit-reset
