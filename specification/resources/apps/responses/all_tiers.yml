Description: A JSON object with a `tiers` key. This will be a list
  of all app tiers

content:
  application/json:
    schema:
      $ref: ../models/apps_list_tiers_response.yml
    example:
      tiers:
        - build_seconds: 800
      
headers:
  ratelimit-limit:
    $ref: ../../../shared/headers.yml#/ratelimit-limit
  ratelimit-remaining:
    $ref: ../../../shared/headers.yml#/ratelimit-remaining
  ratelimit-reset:
    $ref: ../../../shared/headers.yml#/ratelimit-reset

