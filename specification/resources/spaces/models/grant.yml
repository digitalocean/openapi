type: object

properties:
  bucket:
    type: string
    description: The name of the bucket.
    example: my-bucket

  permission:
    type: string
    description: The permission to grant to the user.
    enum:
    - read
    - readwrite
    - fullaccess
    - ''
    example: read

required:
- bucket
- permission
