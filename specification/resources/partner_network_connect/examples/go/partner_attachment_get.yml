lang: Go
source: |-
  import (
      "context"
      "os"

      "github.com/digitalocean/godo"
  )

  func main() {
      token := os.Getenv("DIGITALOCEAN_TOKEN")

      client := godo.NewFromToken(token)
      ctx := context.TODO()

      partnerAttachments, _, err := client.PartnerAttachment.Get(ctx, "f6912795-edb3-4163-b9e3-0770a1e61c43")
  }
