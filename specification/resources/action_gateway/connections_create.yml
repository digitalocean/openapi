operationId: connections_create
summary: Create a Connection
description: 'Creates a connection that authorizes one of your end users to use a provider''s tools. For
  OAuth providers the connection starts as `pending` and the response includes `authorization`: send the
  user to `connect_url`, show them `verification_code`, and poll the connection until it is `active`.'
tags:
- Action Gateway
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: models/connections.yml#/connection_create
      example:
        provider: jira
        user_id: alice
        scopes:
        - write:jira-work
        connection_parameters:
          site_url: https://example.atlassian.net
        credential:
          digitalocean_oauth: {}
responses:
  '200':
    description: The connection was created.
    headers:
      $ref: response_headers.yml
    content:
      application/json:
        schema:
          $ref: models/connections.yml#/connection_response
        example:
          connection:
            id: 39f9f768-a7f9-4c91-9557-3328c53dd054
            provider: jira
            provider_display_name: Jira
            user_id: alice
            status: pending
            created_at: '2026-07-14T00:50:00Z'
            updated_at: '2026-07-14T00:50:00Z'
            revoked_at: null
            connection_parameters:
              site_url: https://example.atlassian.net
            credential_kind: digitalocean_oauth
            credential_id: ''
            network: null
            scopes:
            - write:jira-work
            granted_at: null
            oauth:
              scopes:
              - write:jira-work
              granted_at: null
          authorization:
            status: requires_authorization
            connect_url: https://cloud.digitalocean.com/security/connectlinks/confirm?token=eyJhbGciOi
            verification_code: 97rwxzcy
            expires_at: '2026-07-14T01:00:00Z'
  '400':
    $ref: ../../shared/responses/bad_request.yml
  '401':
    $ref: ../../shared/responses/unauthorized.yml
  '409':
    $ref: ../../shared/responses/conflict.yml
  '429':
    $ref: ../../shared/responses/too_many_requests.yml
  '500':
    $ref: ../../shared/responses/server_error.yml
  default:
    $ref: ../../shared/responses/unexpected_error.yml
x-codeSamples:
- $ref: examples/curl/connections_create.yml
security:
- bearer_auth:
  - action_gateway:create
