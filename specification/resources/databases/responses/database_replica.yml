description: A JSON object with a key of `replica`.

headers:
  ratelimit-limit:
    $ref: '../../../shared/headers.yml#/ratelimit-limit'
  ratelimit-remaining:
    $ref: '../../../shared/headers.yml#/ratelimit-remaining'
  ratelimit-reset:
    $ref: '../../../shared/headers.yml#/ratelimit-reset'

content:
  application/json:
    schema:
      type: object
      properties:
        replica:
          $ref: '../models/database_replica_read.yml'
    example:
      replica:
        name: read-nyc3-01
        connection:
          uri: ''
          database: defaultdb
          host: read-db.example.com
          port: 25060
          user: doadmin
          password: example_password
          ssl: true
        private_connection:
          uri: postgres://doadmin:example_password@private-read-db.example.com:25060/defaultdb?sslmode=require
          database: ''
          host: private-read-db.example.com
          port: 25060
          user: doadmin
          password: example_password
          ssl: true
        region: nyc3
        status: online
        created_at: '2019-01-11T18:37:36Z'
        do_settings:
          service_cnames:
            - "replica-db.example.com"
            - "read-replica.myapp.io"
