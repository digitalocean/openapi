domain_record_a:
  type: object
  allOf:
    - $ref: domain_record.yml
    - required:
        - type
        - name
        - data

domain_record_aaaa:
  type: object
  allOf:
    - $ref: domain_record.yml
    - required:
        - type
        - name
        - data

domain_record_caa:
  type: object
  allOf:
    - $ref: domain_record.yml
    - required:
        - type
        - name
        - data
        - flags
        - tag

domain_record_cname:
  type: object
  allOf:
    - $ref: domain_record.yml
    - required:
        - type
        - name
        - data

domain_record_mx:
  type: object
  allOf:
    - $ref: domain_record.yml
    - required:
        - type
        - data
        - priority

domain_record_ns:
  type: object
  allOf:
    - $ref: domain_record.yml
    - required:
        - type
        - name
        - data
        - flags
        - tag

domain_record_soa:
  type: object
  allOf:
    - $ref: domain_record.yml
    - required:
        - type
        - ttl

domain_record_srv:
  type: object
  allOf:
    - $ref: domain_record.yml
    - required:
        - type
        - name
        - data
        - priority
        - port
        - flags
        - tag

domain_record_txt:
  type: object
  allOf:
    - $ref: domain_record.yml
    - required:
        - type
        - name
        - data
        - flags
        - tag
