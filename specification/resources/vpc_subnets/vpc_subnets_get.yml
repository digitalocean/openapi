operationId: vpcSubnets_get

summary: Retrieve a VPC Subnet

description: |
  To retrieve a specific subnet within a VPC, send a GET request to
  `/v2/vpcs/$VPC_UUID/subnets/$SUBNET_UUID`.

tags:
  - VPC Subnets [Public Preview]

parameters:
  - $ref: 'parameters.yml#/vpc_uuid'
  - $ref: 'parameters.yml#/subnet_uuid'

responses:
  '200':
    $ref: 'responses/vpc_subnets.yml#/single_vpc_subnet'
  '401':
    $ref: '../../shared/responses/unauthorized.yml'
  '404':
    $ref: '../../shared/responses/not_found.yml'
  '429':
    $ref: '../../shared/responses/too_many_requests.yml'
  '500':
    $ref: '../../shared/responses/server_error.yml'
  default:
    $ref: '../../shared/responses/unexpected_error.yml'

x-codeSamples:
  - $ref: 'examples/curl.yml#/get'

security:
  - bearer_auth:
      - 'vpc:read'
