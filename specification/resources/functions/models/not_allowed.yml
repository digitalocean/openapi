type: object

properties:
  error:
    description: A message providing information about the error.
    type: string
    example: invalid

  messages:
    description: A list of error messages.
    type: object
    properties:
      '':
        type: array
        items: 
          type: string
        example: ['not allowed to get namespace']

required:
  - error
  - messages
