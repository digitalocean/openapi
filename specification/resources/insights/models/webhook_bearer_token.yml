type: object
description: Bearer token authentication for a webhook.
required:
  - token
properties:
  token:
    type: string
    format: password
    description: Bearer token value sent in the Authorization header.
    example: secret-token
