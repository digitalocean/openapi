description: Created

headers:
  ratelimit-limit:
    $ref: '../../../shared/headers.yml#/ratelimit-limit'
  ratelimit-remaining:
    $ref: '../../../shared/headers.yml#/ratelimit-remaining'
  ratelimit-reset:
    $ref: '../../../shared/headers.yml#/ratelimit-reset'

content:
  application/json:
    schema:
      oneOf:
      - properties:
          droplet:
            $ref: '../models/droplet.yml'
          links:
            type: object
            properties:
              actions:
                type: array
                items:
                  $ref: '../models/action_link.yml'
      - properties:
          droplets:
            type: array
            items:
              $ref: '../models/droplet.yml'
          links:
            type: object
            properties:
              actions:
                type: array
                items:
                  $ref: '../models/action_link.yml'

    examples:
      Single Droplet Create Response:
        $ref: 'examples.yml#/createResponse'
      Multiple Droplet Create Response:
        $ref: 'examples.yml#/multiCreateResponse'