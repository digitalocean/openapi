operationId: destroy_a_key

parameters:
  ssh_key_identifier: '$response.body#/ssh_key/id'

description: >-
  The `id` value returned in the response can be used as the 
  `ssh_key_identifier` parameter in `DELETE /v2/account/keys/{ssh_key_identifier}`.
